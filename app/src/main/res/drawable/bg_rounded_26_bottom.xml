<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/secondarycolor" /> <!-- Màu nền -->
    <corners
        android:topLeftRadius="0dp"
        android:topRightRadius="0dp"
        android:bottomLeftRadius="26dp"
        android:bottomRightRadius="26dp" /> <!-- Bo góc chỉ phía trên -->
    <stroke
        android:width="1dp"
        android:color="@color/secondarycolor" /> <!-- Viền -->
</shape>
